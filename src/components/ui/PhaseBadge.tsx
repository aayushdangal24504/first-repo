export const PhaseBadge = ({ phase }: { phase: number }) => (
  <span className="rounded-full border border-line/70 bg-elevated/60 px-2 py-0.5 text-[10px] font-bold uppercase tracking-[0.18em] text-muted">
    Phase {phase}
  </span>
);
